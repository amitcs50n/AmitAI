HF_TOKEN=hf_your_token_here
WANDB_DISABLED=true

# Local control plane and server-side Next.js proxy. Generate a unique value;
# never expose it through NEXT_PUBLIC_* variables or browser storage.
AMITAI_LOCAL_API_TOKEN=
AMITAI_API_ORIGIN=http://127.0.0.1:8000
AMITAI_HOST=127.0.0.1
AMITAI_PORT=8000
AMITAI_ALLOW_LAN=0
AMITAI_ENABLE_DEV_DOCS=0

# Local SQLCipher storage. Generate once with secrets.token_hex(32), preserve it
# securely, and never reuse AMITAI_LOCAL_API_TOKEN as the database key.
AMITAI_DB_KEY=
AMITAI_ENCRYPT_EXISTING_DB=0

# Remote inference client (local control plane only). Do not reuse the local token.
AMITAI_REMOTE_INFERENCE_URL=https://your-development-inference-endpoint.example
AMITAI_REMOTE_INFERENCE_TOKEN=

# GPU inference service credential. Configure on that service only and keep it
# distinct from AMITAI_LOCAL_API_TOKEN.
AMITAI_INFERENCE_AUTH_TOKEN=
